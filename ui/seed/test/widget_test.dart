import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Seed app smoke test', (WidgetTester tester) async {
    // Placeholder test — full widget tests require ProviderScope setup.
    expect(1 + 1, equals(2));
  });
}
